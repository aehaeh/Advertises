@page
@model Advertises.Pages.InnerCategories.IndexModel
@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Account/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Add">Create New</a>
</p>
<body>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.InnerCategories[0].Title)
                </th>

                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.InnerCategories)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Title)
                    </td>

                    <td>

                        |
                        <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>

            }
        </tbody>




    </table>
    <div>
        <div>

        </div>
    </div>
</body>