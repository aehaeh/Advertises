@page
@model Advertises.IndexLocalModel
@{
    ViewData["Title"] = "IndexLocal";
    Layout = "~/Pages/Account/_Layout.cshtml";
}

<h1>مدیریت محله </h1>
<p>
    <a asp-page="AddLocal">ایجاد محله جدید</a>
</p>
<body>
    <table class="table rtl">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Locations[0].Name)
                </th>

                <th> حذف </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Locations)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>

                    <td>

                        |
                        <a asp-page="./DeleteLocal" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>
                
            }
        </tbody>

        <a asp-page="./IndexLocal"> Back to List</a>


    </table>
    <div>
        <div>

        </div>
    </div>
</body>
